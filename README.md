Exercise showing type declarations. Note the difference between doing a type
conversion and a function call.

Based on https://github.com/adonovan/gopl.io/tree/master/ch2/tempconv0

Run

```
export GOPATH=$(pwd)
go install main
./bin/main 
```
